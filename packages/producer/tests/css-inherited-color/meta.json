{
  "name": "css-inherited-color",
  "description": "Regression guard for inherited CSS properties (color, font-family, font-size, letter-spacing, line-height) in rendered output. Verifies that text relying on CSS inheritance from a parent is visible after GSAP tl.from() seek completes.",
  "tags": ["regression", "css-inheritance"],
  "minPsnr": 30,
  "maxFrameFailures": 0,
  "minAudioCorrelation": 0,
  "maxAudioLagWindows": 1,
  "renderConfig": {
    "fps": 30,
    "workers": 1
  }
}
